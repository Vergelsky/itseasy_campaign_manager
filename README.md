# Campaign Manager для Keitaro

Веб-приложение для управления распределением офферов по потокам рекламных кампаний в Keitaro.

## Возможности

- ✅ **Авторизация по API ключу Keitaro** — простой вход без паролей
- ✅ **Синхронизация данных** — автоматическая загрузка кампаний, потоков и офферов из Keitaro
- ✅ **Управление офферами** — добавление, удаление и восстановление офферов в потоках
- ✅ **Автоматический пересчёт share** — умное распределение процентов между офферами
- ✅ **Закрепление share** — фиксация значений для отдельных офферов
- ✅ **Визуальные индикаторы** — цветовая подсветка изменений (зелёный для добавленных, серый для удалённых)
- ✅ **Восстановление офферов** — возможность вернуть удалённые офферы
- ✅ **Сохранение закреплений** — закреплённые офферы сохраняются при синхронизации
- ✅ **Статистика кампаний** — клики, конверсии, ROI, прибыль
- ✅ **Toast уведомления** — информативные сообщения о действиях
- ✅ **Проверка расхождений** — автоматическое обнаружение различий с Keitaro
- ✅ **Колонки Stats и Trends** — зарезервированы для будущей функциональности

## Технологии

- **Backend**: Django 5.1.4
- **База данных**: PostgreSQL 15 (с fallback на SQLite для разработки)
- **Контейнеризация**: Docker & Docker Compose
- **Frontend**: Tailwind CSS, jQuery 3.7.1
- **API**: Keitaro Admin API v1

## Быстрый старт

### 1. Клонирование и настройка

```bash
# Клонировать репозиторий (если применимо)
cd itseasy_campaign_manager

# Скопировать .env.example в .env
cp .env.example .env
```

### 2. Настройка переменных окружения

Отредактируйте `.env`:

```env
# Django settings
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database
POSTGRES_DB=campaign_manager
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Keitaro API
KEITARO_URL=https://your-keitaro-instance.com

# Минимальный процент share для оффера (по умолчанию 1)
MIN_SHARE_PERCENT=1
```

**Важно:** Укажите URL вашего Keitaro инстанса в `KEITARO_URL`

### 3. Запуск через Docker

```bash
# Запустить контейнеры
docker-compose up -d --build

# Применить миграции
docker-compose exec web python app/manage.py migrate

# Создать суперпользователя для Django Admin
docker-compose exec web python app/manage.py createsuperuser
```

**Учётные данные:**
- **Основное приложение** (http://localhost:8000): API ключ - любой валидный ключ из Keitaro
- **Django Admin** (http://localhost:8000/admin/): используйте учётные данные, созданные через `createsuperuser`

### 4. Запуск локально (без Docker)

```bash
# Установить зависимости
pip install -r requirements.txt

# Перейти в папку app
cd app

# Применить миграции (с локальной БД SQLite)
$env:POSTGRES_HOST=''; python manage.py migrate

# Создать суперпользователя
$env:POSTGRES_HOST=''; python manage.py createsuperuser

# Запустить сервер
$env:POSTGRES_HOST=''; python manage.py runserver
```

**Учётные данные:**
- **Основное приложение**: любой валидный API ключ Keitaro
- **Django Admin**: используйте учётные данные, созданные через `createsuperuser`

### 5. Открыть в браузере

```
http://localhost:8000
```

## Использование

### Авторизация

1. Откройте приложение
2. Введите ваш API ключ Keitaro
3. API ключ можно получить в Keitaro: `Settings → User Settings → API Key`

### Работа с кампаниями

#### 1. Синхронизация кампаний

- Нажмите **"Синхронизировать с Keitaro"** на главной странице
- Кампании загрузятся из вашего Keitaro инстанса
- Статистика (клики, конверсии, ROI) загружается автоматически

#### 2. Просмотр потоков кампании

- Кликните на название кампании
- Нажмите **"Fetch streams from Keitaro"** для загрузки потоков
- Потоки без офферов отображаются в свёрнутом виде с подсказкой

#### 3. Управление офферами

**Добавление оффера:**
- Начните вводить название оффера в поле поиска
- Выберите оффер из автодополнения
- Нажмите **"Добавить"**
- Share автоматически пересчитается для всех незакреплённых офферов
- Новый оффер подсвечивается зелёным цветом

**Удаление оффера:**
- Нажмите **"Удалить"** рядом с оффером
- Оффер становится серым и остаётся в таблице
- Кнопка меняется на **"Вернуть"**
- Share пересчитывается для оставшихся активных офферов
- Удалённые офферы не отправляются в Keitaro при Push

**Восстановление оффера:**
- Нажмите **"Вернуть"** рядом с удалённым оффером
- Оффер снова становится активным
- Share пересчитывается для всех активных офферов

#### 4. Закрепление share

- Нажмите на булавку 📌 рядом с share оффера
- Закреплённый оффер сохраняет своё значение share при пересчёте
- Закреплённые офферы сохраняются при синхронизации из Keitaro
- Визуально: закреплённая булавка повёрнута на 45° и имеет полную непрозрачность

#### 5. Отправка изменений в Keitaro

- После внесения изменений нажмите **"Push to Keitaro"**
- Изменения отправятся в Keitaro
- Удалённые офферы (disabled) останутся в базе для возможности восстановления
- Или нажмите **"Cancel"** для отмены всех изменений и перезагрузки данных из Keitaro

### Синхронизация с Keitaro

**Автоматическая синхронизация:**
- При синхронизации из Keitaro удалённые там офферы автоматически помечаются как disabled
- Офферы, восстановленные в Keitaro, автоматически активируются локально
- Закреплённые офферы сохраняют своё состояние при синхронизации

**Проверка расхождений:**
- Система автоматически проверяет расхождения с Keitaro
- Предупреждение отображается, если обнаружены различия
- Disabled офферы не учитываются при сравнении

### Django Admin

Доступен по адресу: `http://localhost:8000/admin/`

**Создание суперпользователя:**

```bash
# Через Docker
docker-compose exec web python app/manage.py createsuperuser

# Локально
cd app
$env:POSTGRES_HOST=''
python manage.py createsuperuser
```

**Важно:** 
- При создании суперпользователя запрашивается `api_key` (используется как username) и `password`
- Для входа в основное приложение нужен только API ключ (без пароля)
- Для Django Admin нужны и `api_key` (как username) и `password`

## Структура проекта

```
itseasy_campaign_manager/
├── app/
│   ├── manage.py
│   ├── config/                  # Django settings
│   │   ├── settings.py
│   │   ├── urls.py
│   │   ├── views.py            # Обработчики ошибок (404, 500, 403)
│   │   └── exceptions.py       # Общие исключения
│   ├── users/                  # Авторизация
│   │   ├── models.py           # User модель
│   │   ├── views.py            # LoginView, LogoutView
│   │   ├── middleware.py        # AuthMiddleware
│   │   ├── forms.py
│   │   └── admin.py
│   ├── campaigns/              # Кампании, потоки, офферы
│   │   ├── models.py           # Campaign, Flow, Offer, FlowOffer
│   │   ├── views.py            # CBV для всего функционала
│   │   ├── services.py         # KeitaroClient, SyncService, ShareCalculator
│   │   ├── urls.py
│   │   └── admin.py
│   ├── templates/              # HTML шаблоны
│   │   ├── base.html
│   │   ├── 404.html, 500.html, 403.html
│   │   ├── users/
│   │   └── campaigns/
│   └── static/                 # CSS, JS
│       ├── css/
│       └── js/
│           ├── toast.js        # Система уведомлений
│           └── campaign_detail.js
├── docker-compose.yml
├── Dockerfile
├── entrypoint.sh
├── requirements.txt
├── .env.example
├── PLAN.md                     # Детальный план разработки
└── README.md
```

## API Endpoints

### Веб-интерфейс

- `/` - Главная (список кампаний)
- `/users/login/` - Вход
- `/users/logout/` - Выход
- `/campaigns/` - Список кампаний
- `/campaigns/<id>/` - Детали кампании

### AJAX API

**Синхронизация:**
- `POST /campaigns/sync-campaigns/` - Синхронизация кампаний
- `POST /campaigns/<id>/fetch-streams/` - Синхронизация потоков
- `GET /campaigns/<id>/check-sync/` - Проверка расхождений

**Управление офферами:**
- `POST /campaigns/flow/<id>/add-offer/` - Добавить оффер
- `POST /campaigns/flow-offer/<id>/remove/` - Удалить оффер (пометить как disabled)
- `POST /campaigns/flow-offer/<id>/restore/` - Восстановить оффер
- `POST /campaigns/flow-offer/<id>/toggle-pin/` - Закрепить/открепить share

**Отправка изменений:**
- `POST /campaigns/flow/<id>/push/` - Отправить изменения в Keitaro
- `POST /campaigns/flow/<id>/cancel/` - Отменить изменения

**Вспомогательные:**
- `GET /campaigns/offers/autocomplete/` - Автодополнение офферов
- `POST /campaigns/stats/` - Получить статистику кампаний

## Разработка

### Просмотр логов

```bash
docker-compose logs -f web
```

### Остановка контейнеров

```bash
docker-compose down
```

### Пересборка после изменения requirements.txt

```bash
docker-compose up -d --build
```

### Применение новых миграций

```bash
docker-compose exec web python app/manage.py makemigrations
docker-compose exec web python app/manage.py migrate
```

## Технические детали

### Бизнес-логика распределения share

- Сумма всех активных share должна быть 100%
- Закреплённые (pinned) share не пересчитываются при добавлении/удалении офферов
- Незакреплённые share распределяются равномерно между собой
- Минимальный процент на оффер настраивается через `MIN_SHARE_PERCENT` (по умолчанию 1%)
- При закреплении share учитывается минимальный процент для остальных офферов

### Синхронизация с Keitaro

**Fetch (загрузка из Keitaro):**
- Загружает кампании, потоки и офферы из Keitaro
- Сохраняет закрепления (is_pinned) существующих офферов
- Помечает как disabled офферы, удалённые в Keitaro
- Активирует офферы, восстановленные в Keitaro

**Push (отправка в Keitaro):**
- Отправляет только активные офферы
- Disabled офферы остаются в базе для возможности восстановления
- Валидирует share перед отправкой

**Check Sync (проверка расхождений):**
- Сравнивает только активные офферы
- Disabled офферы не учитываются при сравнении

### Управление состоянием офферов

**Active (активный):**
- Оффер участвует в распределении share
- Отображается нормальным цветом
- Отправляется в Keitaro при Push

**Disabled (удалённый):**
- Оффер не участвует в распределении share (share = 0)
- Отображается серым цветом
- Не отправляется в Keitaro при Push
- Остаётся в базе для возможности восстановления
- Автоматически активируется, если становится активным в Keitaro

### Безопасность

- API ключи хранятся в базе данных (используется встроенный механизм Django)
- CSRF защита для всех POST запросов
- Session-based аутентификация
- Валидация всех пользовательских вводов
- Кастомные страницы ошибок (404, 500, 403)

## Troubleshooting

### Docker не запускается

- Убедитесь что Docker Desktop запущен
- Проверьте что порты 8000 и 5432 свободны

### Ошибка подключения к Keitaro

- Проверьте `KEITARO_URL` в `.env`
- Убедитесь что API ключ валиден
- Проверьте доступность Keitaro инстанса
- Проверьте логи: `docker-compose logs web`

### Миграции не применяются

- Остановите контейнеры: `docker-compose down`
- Удалите volume: `docker volume rm itseasy_campaign_manager_postgres_data`
- Запустите заново: `docker-compose up -d`

### Офферы не отображаются

- Убедитесь что вы нажали "Fetch streams from Keitaro"
- Проверьте что в Keitaro у потока есть офферы
- Потоки без офферов отображаются в свёрнутом виде

## Лицензия

Proprietary

## Поддержка

По вопросам обращайтесь к разработчику.

---

**Campaign Manager** - упрощённая обёртка для Keitaro API с удобным управлением распределением офферов
