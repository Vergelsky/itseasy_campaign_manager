# Campaign Manager для Keitaro

Веб-приложение для управления распределением офферов по потокам рекламных кампаний в Keitaro.

## Основной функционал

- **Авторизация по API ключу Keitaro** — простой вход без паролей
- **Синхронизация данных** — загрузка кампаний, потоков и офферов из Keitaro
- **Управление офферами** — добавление, удаление и восстановление офферов в потоках
- **Автоматический пересчёт share** — умное распределение процентов между офферами
- **Закрепление share** — фиксация значений для отдельных офферов
- **Создание кампаний** — создание новых кампаний с гео-таргетингом и офферами
- **Статистика кампаний** — клики, конверсии, ROI, прибыль
- **Визуальные индикаторы** — цветовая подсветка изменений

## Технологии

- **Backend**: Django 5.1.4
- **База данных**: PostgreSQL 15 (с fallback на SQLite для разработки)
- **Контейнеризация**: Docker & Docker Compose
- **Frontend**: Tailwind CSS, jQuery
- **API**: Keitaro Admin API v1

## Разворачивание проекта

### 1. Настройка окружения

```bash
# Скопировать .env.example в .env
cp .env.example .env
```

Отредактируйте `.env`:

```env
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

POSTGRES_DB=campaign_manager
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=db
POSTGRES_PORT=5432

KEITARO_URL=https://your-keitaro-instance.com
MIN_SHARE_PERCENT=1
```

### 2. Запуск через Docker

```bash
# Запустить контейнеры
docker-compose up -d --build

# Применить миграции
docker-compose exec web python app/manage.py migrate

# Создать суперпользователя для Django Admin
docker-compose exec web python app/manage.py createsuperuser
```

Приложение будет доступно по адресу: **http://localhost:1987**

### 3. Запуск локально (без Docker)

```bash
# Установить зависимости
pip install -r requirements.txt

# Перейти в папку app
cd app

# Применить миграции (с локальной БД SQLite)
$env:POSTGRES_HOST=''; python manage.py migrate

# Создать суперпользователя
$env:POSTGRES_HOST=''; python manage.py createsuperuser

# Запустить сервер
$env:POSTGRES_HOST=''; python manage.py runserver
```

Приложение будет доступно по адресу: **http://localhost:8000**

## Использование

### Авторизация

1. Откройте приложение
2. Введите ваш API ключ Keitaro (можно получить в Keitaro: `Settings → User Settings → API Key`)

### Работа с кампаниями

**Синхронизация:**
- Нажмите **"Синхронизировать с Keitaro"** для загрузки кампаний
- На странице кампании нажмите **"Fetch streams from Keitaro"** для загрузки потоков

**Управление офферами:**
- Добавление: введите название оффера в поле поиска, выберите из списка, нажмите **"Добавить"**
- Удаление: нажмите **"Удалить"** (оффер становится серым, можно восстановить)
- Восстановление: нажмите **"Вернуть"** рядом с удалённым оффером
- Закрепление share: нажмите на булавку 📌 рядом с share

**Отправка изменений:**
- Нажмите **"Push to Keitaro"** для отправки изменений
- Или **"Cancel"** для отмены и перезагрузки данных из Keitaro

**Создание кампании:**
- Нажмите **"Добавить кампанию"** на главной странице
- Заполните форму: название, гео-коды стран (через запятую), выберите оффер
- Система создаст кампанию в Keitaro с двумя потоками: гео-таргетинг на Google и редирект на оффер

## Структура проекта

```
itseasy_campaign_manager/
├── app/
│   ├── manage.py
│   ├── config/              # Django settings, URLs, exceptions
│   ├── users/               # Авторизация (User модель, Login/Logout)
│   ├── campaigns/           # Кампании, потоки, офферы
│   │   ├── models.py        # Campaign, Flow, Offer, FlowOffer
│   │   ├── views/           # View классы (campaign_views, flow_views, offer_views, stats_views)
│   │   ├── services/        # Бизнес-логика (client, calculator, sync_service)
│   │   ├── forms.py
│   │   └── urls.py
│   ├── templates/           # HTML шаблоны
│   └── static/              # CSS, JS
│       └── js/
│           ├── campaign_detail/  # Модули для страницы деталей
│           └── campaign_list.js
├── docker-compose.yml
├── Dockerfile
├── requirements.txt
└── README.md
```

## Разработка

**Просмотр логов:**
```bash
docker-compose logs -f web
```

**Остановка:**
```bash
docker-compose down
```

**Применение миграций:**
```bash
docker-compose exec web python app/manage.py makemigrations
docker-compose exec web python app/manage.py migrate
```

## Технические детали

**Распределение share:**
- Сумма всех активных share = 100%
- Закреплённые share не пересчитываются
- Незакреплённые share распределяются равномерно
- Минимальный процент настраивается через `MIN_SHARE_PERCENT`

**Синхронизация:**
- Удалённые в Keitaro кампании помечаются как `deleted` и не отображаются
- Удалённые в Keitaro офферы помечаются как `disabled` (можно восстановить)
- Закрепления (is_pinned) сохраняются при синхронизации

## Troubleshooting

**Ошибка подключения к Keitaro:**
- Проверьте `KEITARO_URL` в `.env`
- Убедитесь что API ключ валиден

**Миграции не применяются:**
- Остановите контейнеры: `docker-compose down`
- Удалите volume: `docker volume rm itseasy_campaign_manager_postgres_data`
- Запустите заново: `docker-compose up -d`

---

**Campaign Manager** - упрощённая обёртка для Keitaro API с удобным управлением распределением офферов
